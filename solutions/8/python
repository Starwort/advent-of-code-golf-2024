from itertools import*;t=enumerate
G={i+j*1j:c for i,r in t(open(0))for j,c in t(r.strip())}
for r in[1],range(50):print(len({a+n*(a-b)for f in{*G.values()}-{'.'}for a,b in permutations([k for k in G if G[k]==f],2)for n in r if-a*~n-b*n in G}))